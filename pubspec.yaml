name: genzebet
description: GenzeBet financial tracker for Ethiopian market.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.5.1
  intl: ^0.19.0
  crypto: ^3.0.3
  android_sms_reader: ^0.0.6
  shared_preferences: ^2.3.2
  permission_handler: ^11.3.1
  sqflite: ^2.4.3
  path: ^1.9.1
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
